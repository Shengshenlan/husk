"""Quickstart: spawn a sandbox via husk-client.

Status: scaffold. Real example fills in once Phase 1.7 generates the client.
"""

# from husk_client import AuthenticatedClient
# from husk_client.api.sandbox import create_sandbox
#
# client = AuthenticatedClient(base_url="http://localhost:8000", token="hk_...")
# sb = create_sandbox.sync(client=client, body=...)
# print(sb)
