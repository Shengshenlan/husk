/**
 * @husk/client — TypeScript client for the Husk control plane API.
 *
 * The auto-generated path/schema types live in `_generated/schemas.ts`.
 * Use them with `openapi-fetch`:
 *
 *   import createClient from "openapi-fetch"
 *   import type { paths } from "@husk/client"
 *   const client = createClient<paths>({ baseUrl: "http://localhost:8000" })
 *
 * Phase 2.x: a higher-level ergonomic wrapper will live in `./ergonomic.ts`.
 */

// Re-export the generated types once `pnpm gen` populates them.
// export type { paths, components } from './_generated/schemas'

export const VERSION = '0.0.1'
